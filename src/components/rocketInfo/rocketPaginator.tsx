import {Box, Typography} from "@mui/material";

export default async function RocketPaginator() {
    return (
        <Box sx={{ width: 1, height: '100%', backgroundColor: '#00771c'}} >
            <Typography>Rocket Image Pagination</Typography>
        </Box>
    )
}